# -- backend

backend = "glx";
xrender-sync-fence = true;

# -- glx

glx-no-stencil = true;
glx-copy-from-front = false;
glx-no-rebind-pixmap = true;

# -- shadows

shadow = true;
shadow-radius = 10;
shadow-offset-x = -10;
shadow-offset-y = -8;
shadow-opacity = 0.7;

# -- fading

fading = false;
