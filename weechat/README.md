## Weechat config

After installing the dotfiles, you should update the conf files with your irc nick and username. You also need to configure a password for freenode. A passphrase must be set to encrypt the password.

Set a passphrase:

    /secure passphrase <pass>

Set irc password:

    /secure set freenodepass <pass>
